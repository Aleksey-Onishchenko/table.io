{
  "devDependencies": {
    "cssnano": "^4.1.10",
    "gh-pages": "^2.1.1",
    "sass": "^1.23.7"
  },
  "scripts": {
    "build": "parcel build index.html --public-url /table.io",
    "deploy": "gh-pages -d dist",
    "start": "parcel index.html"
  }
}
